{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ambient",
  "description": "Only the window controls required by Ambient ALMA",
  "windows": ["ambient"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
