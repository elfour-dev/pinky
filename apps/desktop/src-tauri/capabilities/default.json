{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the primary Pinky window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
