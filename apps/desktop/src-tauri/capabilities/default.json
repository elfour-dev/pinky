{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the primary Pinky window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
