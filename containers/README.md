# Containers

This directory is reserved for digest-pinned, rootless-Podman images for document
extraction, Chromium, SearXNG, and generated-code execution. Images are not added
until their upstream artifacts, checksums, licenses, and escape/resource tests are
recorded. A floating tag is not an acceptable substitute for a pinned digest.
